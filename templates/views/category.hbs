<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Products - Category</title>
    <link rel="stylesheet" href="/css/nav.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: Arial, sans-serif;
            background-color: #f5f5f5;
            /* Light gray background */
            margin: 0;
            padding: 0;
        }

        .container {
            display: flex;
            align-items: center;
            justify-content: center;
            background-color: #fff;
            padding: 20px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            gap: 100px;
        }

        .h1-heading {
            font-size: 30px;
            color: #2d3e50;
            /* Dark blue text color */
            text-align: center;
            margin-top: 20px;
        }

        li {
            font-size: 24px;
            color: #333;
            /* Dark gray text color */
            margin: 10px 0;
            list-style: circle;
        }

        li a {
            text-decoration: none;
            color: #3498db;
            /* Blue link color */
        }

        li a:hover {
            color: #e74c3c;
            /* Red link color on hover */
        }
    </style>
</head>

<body>
    {{>header}}
    <h1 class="h1-heading">All Categories</h1>
    <div class="container">
        {{#each category}}
        <li><a style="text-transform: capitalize;" href="/products/category/{{this}}">{{this}}</a></li>
        {{/each}}
    </div>
</body>

</html>